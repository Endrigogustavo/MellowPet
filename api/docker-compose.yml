services:
  api:
    build: .
    ports:
      # Só na loopback: quem expõe para a internet é o proxy TLS na frente.
      # Troque para "8000:8000" se for publicar direto (não recomendado).
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    mem_limit: 512m
    security_opt:
      - no-new-privileges:true
