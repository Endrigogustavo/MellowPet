# Dependências só de desenvolvimento/CI.
# Instale junto com requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# A suíte de testes não carrega modelo nenhum: cobre config, segurança e as
# funções puras do motor de emoção. Por isso não precisa de mediapipe nem
# onnxruntime aqui — só do que essas funções tocam (cv2/numpy).
pytest==8.3.3
httpx==0.27.0
fastapi==0.111.0
pydantic==2.12.5
pydantic-settings==2.13.1
email-validator==2.2.0
opencv-python-headless==4.13.0.92
numpy==2.1.3
pylint==3.3.1
