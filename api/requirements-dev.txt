# Dependências só de desenvolvimento/CI.
# Instale junto com requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# A suíte de testes não carrega modelo visual nenhum. Mantemos aqui apenas as
# dependências leves tocadas pelos testes, incluindo autenticação, sem trazer
# mediapipe/onnxruntime e os provedores de IA para o job de CI.
pytest==8.3.3
httpx==0.27.0
fastapi==0.111.0
pydantic==2.12.5
pydantic-settings==2.13.1
email-validator==2.2.0
opencv-python-headless==4.13.0.92
numpy==2.1.3
pylint==3.3.1
bcrypt==4.2.0
pyjwt==2.9.0
aiosqlite==0.20.0
databases[aiosqlite]==0.9.0
sqlalchemy==2.0.44
