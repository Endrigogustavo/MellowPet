<?xml version="1.0" encoding="utf-8"?>
<!-- Progresso circular para RemoteViews: shape=ring com useLevel no item de
     progresso é a única forma de anel determinado num widget. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
  <item android:id="@android:id/background">
    <shape android:shape="ring" android:innerRadiusRatio="2.9"
           android:thicknessRatio="5.5" android:useLevel="false">
      <solid android:color="#F2F2F7" />
    </shape>
  </item>
  <item android:id="@android:id/progress">
    <rotate android:fromDegrees="270" android:toDegrees="270"
            android:pivotX="50%" android:pivotY="50%">
      <shape android:shape="ring" android:innerRadiusRatio="2.9"
             android:thicknessRatio="5.5" android:useLevel="true">
        <solid android:color="#6C63FF" />
      </shape>
    </rotate>
  </item>
</layer-list>
